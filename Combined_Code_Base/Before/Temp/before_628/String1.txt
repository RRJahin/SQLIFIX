"SELECT EMPRESTIMO.*, LIVRO.LIVRO_ID, ESTUDANTE.ESTUDANTE_ID " + "FROM EMPRESTIMO " + "INNER JOIN LIVRO ON (LIVRO.LIVRO_ID = EMPRESTIMO.LIVRO_ID) " + "INNER JOIN ESTUDANTE ON (ESTUDANTE.ESTUDANTE_ID = EMPRESTIMO.ESTUDANTE_ID) " + "WHERE EMPRESTIMO_ID = ?"