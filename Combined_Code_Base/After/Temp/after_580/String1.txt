"Select UserId, `Name`, a.Id as aId, i.Id, `Hash`, `Image`, `Album`, `CreatedIn` From `ALBUM` as a " + "INNER JOIN `IMAGE` as i on i.Album = a.Id" + " Where `UserId` = ? Group by `Name`, i.`Id`, `Album`"